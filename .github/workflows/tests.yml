name: Test Suite

on:
  workflow_call:

permissions:
  contents: read

jobs:
  build:
    name: PHP ${{ matrix.php }} - PHPUnit ${{ matrix.phpunit }}
    runs-on: ubuntu-latest
    env:
      XDEBUG_MODE: coverage

    strategy:
      fail-fast: false
      matrix:
        include:
          # PHPUnit 13 (compatible with PHP 8.4, 8.5)
          - php: '8.4'
            phpunit: '13'
          - php: '8.5'
            phpunit: '13'

          # PHPUnit 12 (compatible with PHP 8.3, 8.4, 8.5)
          - php: '8.3'
            phpunit: '12'
          - php: '8.4'
            phpunit: '12'
          - php: '8.5'
            phpunit: '12'

    steps:
      - uses: actions/checkout@v7

      - name: Setup PHP
        uses: shivammathur/setup-php@v2
        with:
          php-version: ${{ matrix.php }}
          coverage: xdebug

      - name: Check PHP Version
        run: php -v

      - name: Require specific PHPUnit version
        run: composer require --dev "phpunit/phpunit:^${{ matrix.phpunit }}" --no-update --no-interaction

      - name: Validate composer.json
        run: composer validate --strict

      - name: Cache Composer packages
        id: composer-cache
        uses: actions/cache@v5
        with:
          path: vendor
          key: ${{ runner.os }}-php-${{ matrix.php }}-phpunit-${{ matrix.phpunit }}-${{ hashFiles('**/composer.lock') }}
          restore-keys: |
            ${{ runner.os }}-php-${{ matrix.php }}-phpunit-${{ matrix.phpunit }}-

      - name: Install dependencies
        run: composer update --prefer-dist --no-progress

      - name: Run test suite
        run: composer run-script coverage

      - name: Upload Coverage to Coveralls
        uses: coverallsapp/github-action@v2
        with:
          file: coverage.xml
        continue-on-error: true
