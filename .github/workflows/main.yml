name: CI

on:
  push:
    branches: [ "main", "PHPUnit_8", "PHPUnit_9" ]
    paths-ignore:
      - '**.md'
      - '.gitignore'
      - 'LICENSE'
  pull_request:
    branches: [ "main", "PHPUnit_8", "PHPUnit_9" ]
    paths-ignore:
      - '**.md'
      - '.gitignore'
      - 'LICENSE'

permissions:
  contents: read

jobs:
  tests:
    uses: ./.github/workflows/tests.yml
