name: CI

on:
  push:
    branches: [ "main", "PHPUnit_8", "PHPUnit_9" ]
  pull_request:
    branches: [ "main", "PHPUnit_8", "PHPUnit_9" ]

permissions:
  contents: read

jobs:
  tests:
    uses: ./.github/workflows/tests.yml
